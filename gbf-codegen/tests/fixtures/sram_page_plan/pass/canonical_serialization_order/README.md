Pass fixture: commit-boundary serialization order is canonical by
`SramPageBindingId`/`ValueId`, independent of the physical SRAM page order.
The manifest page for same-page manifest residency follows the final
serialized member.
