Golden-style marker for a successful F-B9 SRAM plan that emits `sram_cert.v1`.

The test pins the certificate claim fields for the plan self-hash, single-page
invariant, resolved persist/SRAM-paged bindings, switch cap, and page-rotation
evidence without storing generated hashes in the fixture tree.
