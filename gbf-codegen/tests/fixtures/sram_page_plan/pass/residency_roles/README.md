Pass fixture: Stage 7 maps available persist provenance into the RFC
`SramResidencyRole` variants for continuation, transcript, harness, and trace
payload classes, and the SRAM certificate reports those persistent-kind
counts.
