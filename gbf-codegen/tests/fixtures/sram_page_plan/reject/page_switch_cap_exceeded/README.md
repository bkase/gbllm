Golden-style marker for the F-B9 SRAM page-switch cap rejection.

The fixture projects two SRAM page rotations while the resolved cap allows only
one page switch per token, so Stage 7 must fail with a hard policy-projection
diagnostic and must not emit `sram_cert.v1`.
