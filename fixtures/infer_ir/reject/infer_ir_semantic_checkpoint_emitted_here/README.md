# IIR-Reject-6: InferIrSemanticCheckpointEmittedHere

semantic checkpoint emitted in Stage 3 instead of later anchor consumers. This is a synthetic counterexample descriptor; the gbf-codegen fixture test table owns the minimal typed diagnostic construction.
