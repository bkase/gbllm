# `gbf compile` pipeline gate (gbf_compile_gate.v1)

First model ROM produced by the compiler dataflow (bd-1skgm): checkpoint export -> sha256-verified `ArtifactCore` import -> lowering middle (`lower_infer` narrow model IR, `lower_quant` v0 integer contract, `legalize` device bounds, `kernel_select` V3 weights-as-code) -> banked MBC5 ROM backend -> `gbf compile` CLI. Generated by `cargo run --release -p gbf-bench --bin compile-gate`; every number below is program output at git `fbf116c73f9b5a69bec7cd2ce652cb738fcf0cea`.

## Compiled artifact

- Checkpoint `f_s6_dense_checkpoint_export.v1` (trainer git `71f3f68b65c13d8ec63e1619151c33db5be820b6`), 17 tensors sha256-verified, manifest sha256 `acefecbe5c22c22d`
- Artifact core semantic hash `sha256:b4de5b85c6e85c6d14b4ba116c6cf8d4b20681bc801b0d641d3a7d693cfc9225` (17 canonical tensors); sequence semantics: linear_state(4 bytes/layer) placeholder for stateless_bigram_context
- Program: d_model 64, d_ff 128, 4 blocks, vocab 256, 28 ops; weight zeros 69 permille
- Legalization: 6/6 checks passed; kernel plan: 28 selections (block matvecs -> V3 weights-as-code, tied head -> lane-major i8 product-LUT)
- ROM: 1048576 bytes (34 banks), driver 8215 B, weight code 413125 B in 30 chunks, tables 49152 B, 256-token generation loop

## Gate

- **Sequences: PASS** — 4/4 seeds produced a 256-byte on-device sequence identical to the host integer evaluator
- **Health: PASS** — SP home every token, untouched WRAM unchanged, cycles stable, first/last-token dumps byte-exact, done flag set
- **Committed-evidence cross-check: PASS (byte-identical to the bd-2gc6p evidence)** — per-seed sequence sha256 vs `docs/experiments/multi-token/report.json`

| seed | sequence match | committed sha match | first/last dumps | cycles/token (median) | SP home | WRAM clean |
|---|---|---|---|---|---|---|
| 0x0A | yes | yes | yes/yes | 2729315 | yes | yes |
| 0x20 | yes | yes | yes/yes | 2731716 | yes | yes |
| 0x54 | yes | yes | yes/yes | 2731716 | yes | yes |
| 0x65 | yes | yes | yes/yes | 2731716 | yes | yes |

- Mean over all seeds and tokens: **2731307 M-cycles/token** = 2.605 s/token on DMG

## What is real vs still stubbed

Stages executed with real dataflow on this path:

- import_checkpoint_export (sha256-verified -> ArtifactCore)
- lower_infer (narrow DenseBigramProgram IR; not GbInferIR — see module doc)
- lower_quant (v0 integer numeric contract via gbf-kernel model_ref)
- legalize (device-bound checks with observed values)
- kernel_select (V3 weights-as-code family per bake-off bd-rzq5n)
- rom_backend (gbf-kernel asm_impl_model banked MBC5 builder)

Generic pipeline stages **not** on this path (no real producer yet):

- validate (Stage 0 artifact-view validation)
- storage_plan
- window
- overlay_plan
- arena
- schedule / schedule_cost
- stage_cache (run_compiled_build_stage_cache_pipeline)

- The lowering-middle IR is a narrow model-class IR, not the Stage 3 GbInferIR; constructing GbInferIR would require fabricating Stage 0-2 products that have no real producer for a checkpoint export today.
- ROM section assembly reuses the gbf-kernel model-ROM builder proven byte-exact by the one-token/multi-token gates; gbf-codegen owns selection/orchestration.
