# Dependencies

Production dependencies for `gbf-hw`:

- `gbf-foundation`
- `serde`

Dev dependencies:

- `serde_json`

No dependency on `chrono`, `gbf-bench`, `gbf-emu`, training crates, or benchmarking machinery is introduced.

Reviewer command:

```bash
cargo tree -p gbf-hw
```
