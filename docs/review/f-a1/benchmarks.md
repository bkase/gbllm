# Benchmarks

No F-A1 runtime benchmark is claimed. The implementation is pure host-side assembly and packet generation; the claimed executable gates are tests plus byte-for-byte artifact reproduction.

`artifacts/bench-output.txt` records this non-claim so the packet has an explicit benchmark slot without implying emulator or runtime performance validation.
