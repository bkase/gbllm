# Dependencies

`gbf-asm` uses existing workspace dependencies plus `serde` / `serde_json`. PR3 adds no third-party crate dependency.

`artifacts/cargo-tree.txt` is generated by `build-packet.sh`. `artifacts/cargo-deny.txt` records either `cargo deny check` output or a note that `cargo-deny` was unavailable in the packet build environment.
